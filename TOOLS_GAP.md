# TOOLS_GAP — карта инструментов Директа/Метрики: что Борис недоиспользует

Аудит на утверждение владельца. Контекст: B2B-доставка обедов, **Поиск**, Москва+МО, **ручные ставки** (HIGHEST_POSITION), одна кампания 711897777. North star — максимум заявок на рубль.

**Правило проекта:** ничего не внедряем этим документом. Новые **write**-структуры — ТОЛЬКО после пробы на ТЕСТОВОЙ кампании, незнакомую структуру API не угадывать. Разрешённый write-набор сейчас: `keywordbids.set`, `campaigns.update` (минуса на уровне кампании, DailyBudget, ADD_METRICA_TAG), `keywords.suspend`, `campaigns.suspend`. Объявления — только чтение.

**Что Борис уже умеет (база для колонки «статус»):**
READ Директ — `campaigns.get`, `keywords.get`, `keywordbids.get` (со ставками аукциона), `ads.get`, автотаргет-записи, Reports (`SEARCH_QUERY_PERFORMANCE` + `CUSTOM`). READ Метрика — `stat/v1/data` по цели 575665118 (по дням / источникам / utm / посадочным). WRITE — только разрешённый набор выше. Задел — `offline_conversions/upload` (за флагом).

Легенда статуса: 🟢 знает+использует · 🟡 знает, не использует · 🔴 не знает вообще.

---

## Статус после сессии «Прозрение» (2026-07-03)

**Разведка живого кабинета 711897777** (read-only, через API; кампания создана 2026-06-30,
~3 дня, в карантине; за жизнь 507 показов / 22 клика / **7177 ₽**):

| Инструмент | ФАКТ в кабинете сейчас | Что значит для north star |
|---|---|---|
| **Расписание показов (TimeTargeting)** | **НЕ задано** → крутимся 24/7, вкл. ночь и выходные | 🚩 B2B платит за мёртвое время (выходные/ночь почти без спроса) |
| **Корректировки ставок** | ровно ОДНА: `DESKTOP_ONLY_ADJUSTMENT` (кампания). Нет: мобайл-вниз, демография, гео, аудитории, расписание | 🚩 мобайл сливает без противовеса |
| **Расход по устройствам (Direct)** | DESKTOP 3605 ₽ → 2 заявки; **MOBILE 1672 ₽ / 6 кликов → 0 заявок**; TABLET ~0 | 🚩🚩 **~32% расхода на устройство с 0 заявок** — сильнейший слив |
| **Заявки по устройствам (Метрика)** | PC CR 27%, **Smartphone CR 0%** | подтверждает перекос |
| **Групповые минус-фразы** | у ВСЕХ 4 групп `NegativeKeywords: null` — вся минусовка на уровне кампании (сотни фраз, есть токены `сотрудник`/`для сотрудников`) | 🚩 риск каннибализации группы G8 «Сотрудники» кампейн-минусом |
| **Операторы соответствия** | **0 из 194 ключей** с `" ! + [ ]` — всё широкое соответствие | источник мусорных подстановок (лечится минусовкой/операторами) |
| **Объявления/расширения** | 12 объявлений (3/группа, A/B уже есть), у ВСЕХ сайтлинки + 8 уточнений; нет визитки/бизнес-профиля/цен/картинок | расширения частично закрыты владельцем; остальное — креатив (зона владельца) |
| **Демография (Метрика, мало данных)** | пол: Ж CR 29% / М CR 12%; возраст 18-24 конвертит (n=3) | объёма пока нет — диагноз молчит по порогу |
| **Гео (Метрика)** | таргет групп = регион 1 (Москва+область), корректно; города вне МО в Метрике — шум IP-геолокации, не слив таргетинга | не приоритет |
| **Reports Директа: Device** | ✅ валиден (питает точный ₽ по устройствам) | добавлен как транспорт |
| **Reports Директа: HourOfDay / DayOfWeek** | ❌ НЕвалидные поля (проверено кабинетом) — час/день недели по расходу через Reports недоступны | час — только Метрика (визиты); выходные — из дневного отчёта `Date` |

**Внедрено этой сессией (чтение+диагноз, аддитивно, write-набор НЕ расширён):**
🟢 `bidmodifiers.get`, `adgroups.get`, расширенный `campaigns.get` (TimeTargeting + живой минус-список),
срезы Метрики device/демография/час, транспорт device-отчёта. Диагнозы **SCHEDULE_WASTE, DEVICE_SKEW,
AUDIENCE_WASTE, GROUP_MINUS_GAP** — в дневной разбор + предложения владельцу с цифрами и «жду добро».
Все объёмно-гейтованы (на молодой кампании молчат). Полигонная линейка НЕ изменилась (baseline
byte-identical), т.к. сигналов в текущих сценариях нет — их подкладывает ШАГ 3.

---

## Статус после волны аудита (2026-07-08/09) — что волна ЗАКРЫЛА/ПРОДВИНУЛА

Волна аудита (5 спринтов, `9701b08…f9afc94`) НЕ добавляла новых WRITE-типов (разрешённый
набор прежний). Но по инструментам сдвинулось:

- 🟢 **SQ-отчёт теперь использует `CriterionId`** (merge `b1f990b`): раньше SQ читался без id
  ключа → пофразная экономика матчилась по ТЕКСТУ (broad match: запрос ≠ текст ключа →
  клики/заявки не приписывались ключам). Теперь агрегация head-статистики/защиты конвертера —
  по `CriterionId` из SQ (снапшот `query_criterion_daily`). Поле выверено живым зондом (56/56
  строк совпали с id живых `keywords.get`). Это закрывает недоиспользование SQ-отчёта.
- 🟢 **Интрадей-отчёт расхода `DateRangeType=TODAY`** (merge `f9afc94`): новый read-транспорт
  `buildTodaySpendReportBody` (Date/Clicks/Cost за сегодня). Выверен зондом: TODAY принят,
  готов за 1 поллинг. Питает катастрофа-детектор в watch. (Раздел F, «разрезы Reports».)
- 🟢 **`campaigns.suspend` (аварийный) — ВПЕРВЫЕ ЗАДЕЙСТВОВАН** (merge `f9afc94`): был в
  разрешённом наборе, но мёртвым кодом; теперь его дёргает катастрофа-детектор при расходе
  ≥1.5× живого DailyBudget (через write-gate). Возобновление — только владелец.
- **Учёт стоимости LLM исправлен** (merge `4396a96`): тариф opus 5/25 (был завышен ×3), кеш-
  токены учитываются. К инструментам Директа/Метрики не относится, но снимает искажение бюджета.

**Живые гэпы (волна НЕ трогала — по-прежнему на утверждение/пробу):** временной таргетинг
(write после пробы), device/гео/демо-корректировки (write после пробы), **групповые минус-фразы
(`adgroups.update`) — ВСЁ ЕЩЁ ВНЕ разрешённого write-набора** (проба B в `PROBES.md`), операторы
соответствия, `changes.check` (не угадываем структуру), FORM_DROPOFF/BOT/AD_TEXT (нужна цель-шаг
Метрики), офлайн-конверсии, оплата за конверсии (развилка владельца).

---

## A. Таргетинг показов и корректировки ставок (крупнейший недобор)

| Инструмент и что даёт для north star | Статус | Предложение (влияние / риск write) |
|---|---|---|
| **Временной таргетинг** (`TimeTargeting` в campaigns.get/update — расписание показов по часам/дням). У нас B2B: **выходные почти мёртвые**, ночь мёртвая. Резать/занулять показы вне рабочего времени = прямая экономия бюджета на пустой трафик → дешевле заявка. | 🟢 чтение+диагноз (SCHEDULE_WASTE) · write после пробы | **СДЕЛАНО (чтение TimeTargeting + диагноз выходного слива), write после пробы.** Влияние **ВЫСОКОЕ** (полигон уже показывает: выходные дают ~10% спроса, но бюджет тратится). Риск write: средний (`campaigns.update`, обратимо; после апдейта вернуть ADD_METRICA_TAG). Проба: включить расписание на тестовой кампании, сверить экономию. |
| **Корректировки ставок — устройства** (`bidmodifiers`, тип Mobile/Desktop/Tablet). B2B-заявки чаще с десктопа (офис-менеджеры); мобайл часто дороже и хуже. Понижающая корректировка на мобайл → меньше расход, та же заявка. | 🟢 чтение+диагноз (DEVICE_SKEW) · write после пробы | **СДЕЛАНО (bidmodifiers.get + device-срез Метрики + транспорт device-отчёта; кабинет: мобайл 1672 ₽/0 заявок), write после пробы.** Влияние **ВЫСОКОЕ**. Риск: средний (`bidmodifiers.add` возвращает "Ids"-массив; отдельная сущность, обратима). Нужен device-разрез отчётов, которого Борис пока не тянет. |
| **Корректировки — гео внутри региона** (bidmodifiers по районам Москвы/городам МО). Зоны с лучшей конверсией — вверх, слабые — вниз. | 🟡 читает (bidmodifiers.get + regionCity Метрики), диагноза нет | **Чтение есть, диагноз позже (объёма на район нет).** Влияние **СРЕДНЕЕ** (гео-разрез заявок нужен, объёмов на район может не хватать → осторожно с шумом). Риск write: средний. |
| **Корректировки — пол/возраст** (демография). Отсечь заведомо нецелевые сегменты. | 🟢 чтение+диагноз (AUDIENCE_WASTE, порог высокий) · write после пробы | **СДЕЛАНО (gender/ageInterval Метрики + bidmodifiers; порог высокий — B2B ЛПР размазаны), write после пробы.** Влияние **НИЗКОЕ-СРЕДНЕЕ** для B2B (ЛПР размазаны по демографии). Риск: средний. |
| **Корректировки — аудитории/ретаргетинг на Поиске** (bidmodifiers по сегментам Метрики/ретаргет-спискам: были на сайте, похожие). | 🟡 читает (bidmodifiers.get), диагноза нет | **Позже.** Влияние **СРЕДНЕЕ** (на чистом Поиске B2B ограничено; сильнее в РСЯ, которая выключена). Риск: средний. |
| **Сезонные корректировки** (SeasonalBidModifiers — на прогнозируемые всплески/спады спроса). | 🔴 не знает | **Не сейчас.** Влияние **НИЗКОЕ** на нашем объёме (данных на сезонность не хватит). Не нужно до накопления истории. |

---

## B. Минус-фразы и соответствие ключей

| Инструмент | Статус | Предложение |
|---|---|---|
| **Минус-фразы на уровне ГРУПП** (`adgroups.update` NegativeKeywords). Сейчас Борис минусует ТОЛЬКО на уровне кампании → минус для группы A может задеть живые запросы группы B (каннибализация — это ровно сценарий T20/T22 полигона). Групповой минус режет точечно. | 🟢 чтение (adgroups.get) + диагноз (GROUP_MINUS_GAP) · write после пробы | **СДЕЛАНО (adgroups.get читает групповые минуса; диагноз ловит кампейн-минус, задевающий конвертящий запрос группы), write после пробы.** Влияние **СРЕДНЕЕ-ВЫСОКОЕ** (снимает риск каннибализации, точнее чистка). Риск write: средний (`adgroups.update`; нужен `adgroups.get` для чтения структуры групп, которого Борис пока не тянет). |
| **Операторы соответствия в ключах** (кавычки `"…"` фиксируют состав, `!` фиксирует словоформу, `+` фиксирует стоп-слова). Уточнение широкого соответствия → меньше мусорных подстановок → меньше пустого расхода → дешевле заявка. | 🟡 читаем тексты ключей (разведка: **0 из 194** операторов — всё широкое) | **Предложением владельцу / write после пробы.** Влияние **СРЕДНЕЕ-ВЫСОКОЕ** (широкое соответствие — источник мусора; операторы бьют по корню). Риск write: **выше среднего** — это правка ТЕКСТА ключа (`keywords.add`/пересоздание, keywords не редактируются напрямую — только suspend+add), легко сузить трафик слишком сильно. Только после пробы. |
| **Типы соответствия / автотаргетинг** (autotargeting сейчас придушен EXACT 0.30 ₽). Управление режимами автотаргета. | 🟡 читает записи автотаргета, не управляет | **Диагноз сейчас, управление предложением.** Влияние **НИЗКОЕ** (уже придушен). Риск: низкий на чтении. |

---

## C. Объявления и расширения (в основном зона владельца — креатив)

| Инструмент | Статус | Предложение |
|---|---|---|
| **Несколько объявлений на группу для A/B** (ads.add; Яндекс сам ротирует и выбирает лучшее по CTR). Выше CTR → выше позиция при той же ставке / дешевле клик → дешевле заявка. | 🟡 читает `ads.get`, не создаёт | **Диагноз CTR сейчас (Борис видит низкий CTR при норм. позиции → флаг «дело в тексте»), создание — ПРЕДЛОЖЕНИЕМ владельцу.** Влияние **СРЕДНЕЕ** (косвенно на цену заявки). Риск write: **высокий по смыслу** — креатив = решение владельца, вне разрешённого набора. Оставить как read+диагноз (это backlog-диагноз AD_TEXT_PROBLEM). |
| **Расширения/трафареты** (быстрые ссылки, уточнения, визитка, цены). Больше места в выдаче → выше CTR. | 🟡 разведка: сайтлинки + 8 уточнений ЕСТЬ у всех 12 объявлений; визитки/бизнес-профиля/цен/картинок НЕТ | **Чтение статуса + диагноз, заполнение — предложением владельцу.** Влияние **НИЗКОЕ-СРЕДНЕЕ**. Риск write: креатив → владелец. |
| **Видео/картинки в Поиске** (графические/видео-дополнения). | 🔴 не знает | **Не приоритет.** Влияние **НИЗКОЕ** для текстового B2B-Поиска. Риск: креатив → владелец. Не нужно сейчас. |

---

## D. Метрика (диагностика первопричин — питает north star через качество решений)

| Инструмент | Статус | Предложение |
|---|---|---|
| **Воронка форм / достижимость формы** (`formReachRate`, сегменты «дошёл до формы / отправил»). Отличает FORM_DROPOFF (визиты качественные, но форма/оффер ломает конверсию — резать трафик НЕЛЬЗЯ) от DATA_NO_CONV (нецелевой трафик). | 🔴 остаётся — нужна цель «дошёл до формы» в Метрике | **РАЗВЕДКА: у счётчика ОДНА цель «Заявка» (=отправка формы). Без отдельной цели/шага «дошёл до формы» истинный formReachRate не посчитать.** Добавлены прокси-срезы (bounceRate/глубина по device — только чтение). Диагноз FORM_DROPOFF ждёт создания владельцем цели-шага в Метрике (это предложение). Риск: нулевой. |
| **Сегменты Метрики** (по устройству/гео/поведению — как основа для bidmodifiers и диагнозов). | 🟢 чтение (device / gender+age / hour срезы) | **СДЕЛАНО:** срезы устройства/демографии/часа изолированы по рекламному трафику (`lastTrafficSource=='ad'`), питают DEVICE_SKEW/AUDIENCE_WASTE. Риск: нулевой. |
| **Отчёт по поисковым фразам Метрики** (кросс-проверка к SEARCH_QUERY Директа). | 🟡 близко (тянет utm-срез), но не фраз-отчёт | **Чтение для сверки.** Влияние **НИЗКОЕ-СРЕДНЕЕ** (дублирует Директ; полезно для DATA_MISMATCH). Риск: нулевой. |
| **Офлайн-конверсии** (`offline_conversions/upload`, заложено за флагом). Замыкает петлю: оптимизация на РЕАЛЬНЫЕ СДЕЛКИ (заявка→оплата), а не только отправку формы — это и есть истинный north star. | 🟡 заложено, не активно | **Активировать, когда пойдут данные сделок (lead→deal в CRM).** Влияние **ВЫСОКОЕ** (истинная цель — деньги, не формы). Риск write: средний (нужен scope `metrika:write`; данные сделок — от владельца/CRM). |
| **Вебвизор / карты кликов/скроллинга** (UI-инструменты диагностики UX). | 🔴 не знает | **Не автоматизировать.** Влияние **НИЗКОЕ** для автономных решений (это ручная UX-аналитика). API-доступ ограничен, машинных решений не даёт. Не нужно. |
| **Электронная торговля (ecommerce)** | 🔴 не знает | **Не нужно** — у нас lead-gen (заявка на доставку), а не корзина/каталог. |

---

## E. Стратегии назначения ставок (крупная развилка — только владелец)

| Инструмент | Статус | Предложение |
|---|---|---|
| **Оплата за конверсии / автостратегия по цене конверсии** (CPA-стратегия вместо ручных ставок HIGHEST_POSITION). Платишь за заявку, не за клик — прямой north star, снимает с Бориса ручной биддинг. В планах по ТЗ. | 🔴 не знает (мы на ручных) | **Развилка владельца.** Влияние **ВЫСОКОЕ**, но это СМЕНА ПАРАДИГМЫ: автостратегия забирает управление ставками у Бориса → его роль смещается к минусам/структуре/диагнозу. Требует достаточного потока конверсий для обучения (у нас пока мало). Риск: высокий (структурный). Не раньше накопления конверсий + решения владельца. |
| **Пакетные стратегии** (общий бюджет/стратегия на несколько кампаний). | 🔴 не знает | **Не сейчас** — у нас одна кампания. Влияние НИЗКОЕ до появления второй волны. |

---

## F. Служебное / отчёты, уже доступные в API, но недоиспользованные

| Инструмент | Статус | Предложение |
|---|---|---|
| **Разрезы Reports по device/geo/demographic/adgroup**. | 🟢 device-отчёт (транспорт) · ⚠️ hour/dayofweek НЕвалидны | **РАЗВЕДКА:** поле `Device` в Reports валидно — билдер device-отчёта добавлен (точный ₽ по устройствам). Поля `HourOfDay`/`DayOfWeek` кабинет отверг (код 8000) — час/день-недели по расходу через Reports НЕДОСТУПНЫ (час — только Метрика/визиты, выходные — из дневного `Date`-отчёта). Device-отчёт в дневной пайплайн пока не подключён (DEVICE_SKEW питается Метрикой). |
| **`adgroups.get`** — чтение структуры/настроек групп. Нужен для групповых минусов (раздел B) и точной диагностики. | 🟢 добавлено | **СДЕЛАНО:** читает группы + `NegativeKeywords` уровня группы (кабинет: у всех 4 групп `null`). Включает раздел B (GROUP_MINUS_GAP). Риск: нулевой. |
| **`changes.check`** — детект внешних правок владельца. | 🔴 отложено (не угадываем структуру) | **НЕ добавлено сознательно:** точная структура запроса/ответа не выверена боевой пробой, инвариант запрещает угадывать API. Ждёт подтверждённого read-зонда; ни один из 4 диагнозов не блокирует. |
| **`dictionaries` (GetRegions и пр.)** — гео-дерево для гео-корректировок. | 🔴 не знает | **По мере надобности** (когда дойдём до гео-bidmodifiers). Риск: нулевой. |

---

## Сводка приоритетов (на утверждение)

**Внедрять первыми (высокое влияние, низкий/средний риск, чтение+диагноз):**
1. **Временной таргетинг** — расписание показов (выходные/ночь). Чтение+диагноз сразу, write после пробы.
2. **Форм-воронка Метрики** (formReachRate по группам) — диагнозы FORM_DROPOFF/AD_TEXT_PROBLEM/BOT_TRAFFIC (backlog #3, только чтение).
3. **Корректировки ставок по устройствам** + device-разрез Reports — чтение+диагноз, write после пробы.
4. **Групповые минус-фразы** + `adgroups.get` — снять каннибализацию; write после пробы.

**Средний приоритет:** операторы соответствия ключей (write после пробы), гео-корректировки, офлайн-конверсии (активировать по данным сделок), device/geo-разрезы Reports, `changes.check`.

**Развилки владельца (крупные, не Борис решает):** переход на оплату за конверсии (смена парадигмы биддинга); создание/A-B объявлений и расширений (креатив).

**Не нужно:** ecommerce, вебвизор-автоматизация, видео/картинки в Поиске, пакетные/сезонные стратегии (на текущем объёме одной кампании).

**Общий принцип:** сначала расширить ЧТЕНИЕ+ДИАГНОЗ (нулевой риск, сразу усиливает мозг и полигон), затем write-инструменты — по одному, каждый через пробу на тестовой кампании, с добавлением в разрешённый набор write-gate только после подтверждения.
