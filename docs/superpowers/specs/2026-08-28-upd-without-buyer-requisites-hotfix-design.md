# УПД без реквизитов покупателя — дизайн hotfix

## Цель

Кнопка «УПД» в списке заказов должна одним нажатием выпустить и открыть УПД выбранного клиента за выбранный день, даже если у покупателя не заполнены юридическое название, ИНН, КПП, ОГРН, юридический адрес, банк и договор. Продавец, ставка НДС, нумерация и аудит при этом должны оставаться однозначными и сохранёнными.

## Подтверждённая причина

Проблема состоит из двух независимых барьеров:

1. `UpdClientButton` сразу открывает GET `/production/print/upd/pdf?clientId=...&date=...`. Этот GET только читает уже созданные `UpdDocument`; при нулевой выборке он возвращает текст с фотографии и никогда не вызывает выпуск.
2. Валидация клиента считает `defaultOurLegalEntityId` частью реквизитов покупателя. Поэтому без ИНН нельзя сохранить нашего продавца, новые заказы получают `Order.ourLegalEntityId = null`, а генератор УПД исключает их.

Сам PDF уже поддерживает пустые реквизиты покупателя: название заменяется `Client.name`, адрес и ИНН — прочерками. `UpdBuyerSnapshot` и поля `Client` nullable; схема `UpdDocument` требует только целые JSON-снапшоты и конкретного нашего продавца.

## Рассмотренные варианты

### 1. Изменить только валидацию клиента

Плюс: минимальная правка. Минусы: кнопка продолжит печатать только ранее выпущенное; уже созданные заказы сохранят `ourLegalEntityId = null`; пользователь должен вручную исправлять клиента, заказ и отдельно выпускать УПД. Требование с фотографии не закрывается.

### 2. Выпускать УПД внутри GET PDF

Плюс: ссылка остаётся простой. Минусы: GET начинает менять БД и выдавать номера документов; повторные открытия, префетч и внешние проверки получают скрытый side effect. Ошибки выпуска невозможно нормально показать в исходной странице. Вариант отклонён.

### 3. Подготовить УПД Server Action, затем открыть read-only PDF — выбран

Клиентская кнопка синхронно открывает пустую вкладку, вызывает авторизованный Server Action через POST и только после успешного идемпотентного выпуска направляет вкладку на существующий GET PDF. Action получает `date + clientId`, поэтому не выпускает документы других клиентов.

## Архитектура и поток данных

1. Валидация `Client` разрешает `defaultOurLegalEntityId` при пустом ИНН. Остальные частично заполненные реквизиты покупателя по-прежнему запрещены.
2. `generateAndGetUpdForDate(dateIso, clientId?)` сохраняет старый пакетный режим без `clientId` и добавляет узкий режим клиента.
3. Узкий режим подготавливает старые заказы через `prismaDirect` в одной interactive-транзакции с уровнем `Serializable`, чтобы выбор продавца и фактическое назначение были одной согласованной операцией. Прямое соединение обязательно по production-контракту Neon этого репозитория: callback-транзакции через pooler могут завершаться `P2028`. Конфликт сериализации Prisma `P2034` повторяет всю транзакцию не более трёх раз; остальные ошибки не retry-ятся.
4. Внутри этой транзакции продавец разрешается в строгом порядке:
   - активный `Client.defaultOurLegalEntityId`;
   - если его нет — единственное активное `OurLegalEntity`;
   - если активных продавцов ноль или больше одного — выпуск прекращается с понятной ошибкой, без номера УПД и без изменения заказов.
5. Транзакция повторно проверяет client/date/status/null-seller и назначает продавца через возвращающий строки update. Default клиента сохраняется только если реально назначен хотя бы один заказ; `ActivityLog` содержит только фактически изменённые order IDs. Уже выбранного продавца она никогда не меняет. Это допустимо и после lock: заказ с `ourLegalEntityId = null` физически не мог попасть в `UpdDocument`.
6. Существующий генератор группирует только выбранного клиента, через `prismaDirect` атомарно получает номер, сохраняет снапшоты и переиспользует документы по бизнес-ключу. Конкурентный повтор остаётся защищён `P2002`. Перед success узкий режим подтверждает, что каждый актуальный eligible-заказ покрыт связью с `UpdDocument` того же клиента и дня; любой непокрытый/привязанный к старой дате заказ даёт читаемую ошибку, даже если у клиента есть другой УПД текущего дня.
7. Кнопка закрывает пустую вкладку и показывает toast при бизнес-ошибке/исключении; при покрытых конфликтах показывает warning, затем при успехе открывает read-only PDF.

## Целостность и ошибки

- `UpdDocument.ourLegalEntityId` не становится nullable.
- При двух активных продавцах код ничего не угадывает.
- Автозаполнение в `Serializable`-транзакции повторяет весь eligibility-фильтр и условие `ourLegalEntityId: null`, поэтому не перетирает параллельное/ручное назначение и не меняет отменённый/перенесённый заказ.
- Default клиента и audit создаются только после фактического назначения; audit перечисляет только возвращённые update-операцией IDs.
- Нумерация остаётся внутри существующей транзакции `getNextDocumentNumber`.
- Bounded retry preparation не дублирует номер УПД: нумерация начинается только после успешно завершённой подготовки и сохраняет существующую защиту `P2002`.
- Все callback-транзакции этого flow используют `DIRECT_URL`; pooled-клиент обслуживает только обычные запросы.
- Success узкого режима означает полный печатаемый комплект: каждый eligible order связан с УПД выбранного клиента/даты.
- GET PDF остаётся read-only и сохраняет старые режимы `id`, `date`, `clientId + date`.
- Пустые реквизиты покупателя замораживаются как `null`; PDF явно рендерит fallback.
- Изменений схемы БД и миграций нет.

## Проверка

- RED/GREEN: создание клиента без ИНН, но с нашим продавцом.
- RED/GREEN: client/date-генерация фильтрует одного клиента и создаёт УПД с пустыми реквизитами покупателя.
- RED/GREEN: старый заказ без продавца получает сохранённого/единственного продавца транзакционно; неоднозначный продавец возвращает ошибку без выпуска.
- RED/GREEN: конкурентно выбывший заказ не меняет default/audit, частичный update логирует только фактические IDs, а fully-linked группа без текущего документа возвращает ошибку.
- Характеризационный render-smoke: `renderToBuffer` создаёт PDF с полностью пустыми реквизитами покупателя.
- Targeted Vitest, полный Vitest, `tsc --noEmit`, Prisma validate/generate, production `next build`, scoped diff.
- Push exact branch SHA в `main`, ожидание статуса Vercel, HTTP smoke канонического домена и GitHub daily smoke.

## Защита от сжатия контекста

Рабочая ветка: `codex/fix-upd-no-legal-data`.

Изолированный каталог: `/Users/macbook/Documents/CRM_FOOD/.worktrees/fix-upd-no-legal-data`.

Базовый SHA: `fa4f10c8e66b5444713d3bae4e8a0c03ab882745` (`origin/main`). Базовый полный Vitest в worktree: 171 файлов, 2134 теста, 0 падений.

Production: Git push в `origin/main` запускает Vercel; проверять `https://budni-crm.vercel.app`. Локально не запускать `npm run vercel-build`, потому что он содержит `prisma migrate deploy`.
